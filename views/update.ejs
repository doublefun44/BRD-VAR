<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DCPR Dates</title>
 <style>
.grid-container {
  display: grid;
  grid-template-columns: 200px 180px 180px 200px 200px 200px 200px 200px 180px 180px 180px auto;
  /* display: flex; */
}

.snDate {
  flex:1;
  gap:10px;
  font-size:20px;
  text-align: center;
}

.sn-title {
  font-weight: bold;
}

body {
    background-color: rgb(217, 235, 214);
}

.btn {
  height: 30px;
  background-color: rgb(158, 126, 189);
}

 </style>

</head>
<body>
    <h1>Layout in ServiceNow Side After VAR sent info</h1>
<hr />

<div class="grid-container sn-title">
    <div class="snDate"><label>Project Name</label></div>
    <div class="snDate"><label>Requested Delivery Date</label></div>
    <div class="snDate"><label>VAR committed ship date</label></div>
    <div class="snDate"><label>VAR committed delivery date</label></div>
    <div class="snDate"><label>VAR revised ship date</label></div>
    <div class="snDate"><label>VAR revised delivery date</label></div>
    <div class="snDate"><label>VAR revised Reason</label></div>
    <div class="snDate"><label>Approve ?</label></div>
    <div class="snDate"><label>Carrier</label></div>
    <div class="snDate"><label>tracking</label></div>
    <div class="snDate"><label>Due Date</label></div>
  </div>
  
  

  <div class="grid-container">
    <div class="snDate">
      <label><p>
      <%  if(locals.projects) { %> 
       <%= projects.project %>   
        <% } else { %>
          <p>nothing</p>
       <% } %> 
      </p></label>
    </div>
    <div class="snDate">
      <%  if(locals.projects) { %> 
        <%= projects.requested %>   
         <% } else { %>
           <p>nothing</p>
        <% } %> 
    </div>
       <div class="snDate"><label>
      <%  if(locals.projects) { %> 
        <%= projects.commitship %>   
         <% } else { %>
           <p>nothing</p>
        <% } %> 
    </label></div>
    <div class="snDate"><label>
      <%  if(locals.projects) { %> 
        <%= projects.commitdeliver %>   
         <% } else { %>
           <p>nothing</p>
        <% } %> 
    </label></div>
    <div class="snDate"><label>   -   </label></div>
    <div class="snDate"><label>-</label></div>
    <div class="snDate"><label> <%  if(locals.projects) { %> 
      <%= projects.reasons %>   
       <% } else { %>
         <p>nothing</p>
      <% } %> </label></div>
    <div><button class="btn">Approve/Reject</button></div>
    <div class="snDate"><label>carrier name</label></div>
    <div class="snDate"><label>tracking number</label></div>
    <div class="snDate"><label><%  if(locals.projects) { %> 
      <%= projects.commitdeliver %>   
       <% } else { %>
         <p>nothing</p>
      <% } %> </label></div>
  </div>
 
     
  </html>